# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @server/auth_workSpace`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t server-auth_workspace`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system server-auth_workspace && \
          adduser --system -G server-auth_workspace server-auth_workspace

COPY apps/auth_workSpace/dist server-auth_workspace/
COPY apps/auth_workSpace/package.json server-auth_workspace/
RUN chown -R server-auth_workspace:server-auth_workspace .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix server-auth_workspace --omit=dev -f install

CMD [ "node", "server-auth_workspace" ]
